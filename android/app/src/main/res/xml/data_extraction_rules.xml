<?xml version="1.0" encoding="utf-8"?>
<!-- HIT-097. Nothing the app stores on the device leaves it through cloud
     backup or device-to-device transfer. SECURITY.md, "Android backup", has
     the reason: a signed-in session must not arrive on a phone that never
     signed in.

     Every storage domain is excluded by name instead of relying on root alone,
     so the intent can be read straight off this file. An exclude takes
     precedence over an include, so a line added here later cannot quietly
     widen it. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="./"/>
        <exclude domain="file" path="./"/>
        <exclude domain="database" path="./"/>
        <exclude domain="sharedpref" path="./"/>
        <exclude domain="external" path="./"/>
        <exclude domain="device_root" path="./"/>
        <exclude domain="device_file" path="./"/>
        <exclude domain="device_database" path="./"/>
        <exclude domain="device_sharedpref" path="./"/>
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="./"/>
        <exclude domain="file" path="./"/>
        <exclude domain="database" path="./"/>
        <exclude domain="sharedpref" path="./"/>
        <exclude domain="external" path="./"/>
        <exclude domain="device_root" path="./"/>
        <exclude domain="device_file" path="./"/>
        <exclude domain="device_database" path="./"/>
        <exclude domain="device_sharedpref" path="./"/>
    </device-transfer>
</data-extraction-rules>
