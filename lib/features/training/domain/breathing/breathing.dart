/// The guided breathing engine (HIT-029).
///
/// One import for callers, the way `models/models.dart` works for the exercise
/// models. The breathing UI (HIT-030) is the first consumer.
library;

export 'breathing_engine.dart';
export 'breathing_timeline.dart';
