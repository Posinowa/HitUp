name: Flutter CI

on:
  pull_request:
    branches: [develop, main]
  push:
    branches: [develop, main]

concurrency:
  group: flutter-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

# The Flutter version lives in `.fvmrc` and nowhere else. Every setup step
# below reads it through `flutter-version-file`, and so does
# release_validation.yml, so the two workflows cannot drift onto different
# SDKs. `repository-policy` checks that the file still says what it should; it
# is a required check, so a broken one cannot be merged.
#
# Pinned on purpose. With only `channel: stable`, CI follows whatever Flutter
# released most recently, so a Flutter release can turn every open PR red on a
# day nobody changed anything. Upgrading is a deliberate chore(toolchain) PR,
# never something that happens on its own.

jobs:
  repository-policy:
    name: repository-policy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Validate repository policy
        shell: bash
        run: |
          set -euo pipefail
          fail=0

          # Forbidden custom-server directories at repo root
          for d in backend server functions cloud-functions; do
            if [ -d "$d" ]; then
              echo "::error::Forbidden server directory detected: $d/"
              fail=1
            fi
          done

          # Branch naming for feature PRs into develop (advisory hard-fail for clearly wrong targets is handled by branch-policy on main)
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            base="${{ github.base_ref }}"
            head="${{ github.head_ref }}"
            echo "PR base=$base head=$head"
            if [ "$base" = "develop" ]; then
              case "$head" in
                feature/*|fix/*|chore/*|hotfix/*|dependabot/*|chore/github-governance) ;;
                develop|main)
                  echo "::error::Invalid head branch for develop PR: $head"
                  fail=1
                  ;;
                *)
                  echo "::warning::Unusual branch name '$head'. Prefer feature/HIT-XXX-description"
                  ;;
              esac
            fi
          fi

          exit "$fail"

      - name: Enforce the presentation layer boundary
        shell: bash
        run: |
          set -euo pipefail

          # ARCHITECTURE.md hard rule 1: presentation widgets must not touch
          # FirebaseAuth or FirebaseFirestore directly, they go through a
          # repository. CONTRIBUTING.md repeats it and the PR template carries
          # a checkbox for it. A checkbox the author ticks is a promise; this
          # is the part that verifies.
          #
          # The tree is clean today, so this costs no remediation. It is much
          # cheaper to hold an invariant than to restore one.
          allowlist='.github/presentation-boundary-allowlist.txt'

          mapfile -t targets < <(
            find lib/app lib/features -type d -name presentation -prune -print 2>/dev/null
            find lib/app -type f -name '*.dart' 2>/dev/null
          )

          fail=0
          for pattern in 'FirebaseFirestore.instance' 'FirebaseAuth.instance' 'FirebaseStorage.instance'; do
            while IFS= read -r hit; do
              [ -z "$hit" ] && continue
              file="${hit%%:*}"
              if [ -f "$allowlist" ] && grep -qxF "$file" "$allowlist"; then
                echo "::notice::Allowlisted presentation boundary exception: $file"
                continue
              fi
              # File and line only. Never echo the surrounding source.
              echo "::error file=${file},line=$(echo "$hit" | cut -d: -f2)::Presentation layer reaches a data source directly ($pattern). Call it through a repository instead. See docs/architecture/ARCHITECTURE.md, hard rule 1."
              fail=1
            done < <(
              grep -rn --include='*.dart' -F "$pattern" lib/app lib/features/*/presentation 2>/dev/null | cut -d: -f1,2 || true
            )
          done

          if [ "$fail" -eq 0 ]; then
            echo "Presentation layer does not reach any data source directly."
          fi
          exit "$fail"

      - name: Validate the pinned Flutter version
        # Also run by release_validation.yml. The reasoning lives in the script.
        run: bash .github/scripts/check-flutter-version.sh

  secret-guard:
    name: secret-guard
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Scan for forbidden credential-like paths
        shell: bash
        run: |
          set -euo pipefail
          # Report paths only — never print file contents
          mapfile -t hits < <(
            find . -type f \
              ! -path './.git/*' \
              ! -path './build/*' \
              ! -path './.dart_tool/*' \
              \( \
                -name '.env' -o \
                -name '.env.*' -o \
                -name '*.p8' -o \
                -name '*.p12' -o \
                -name '*.pfx' -o \
                -name '*.jks' -o \
                -name '*.keystore' -o \
                -name 'service-account*.json' -o \
                -name 'firebase-admin*.json' -o \
                -name '*-firebase-adminsdk-*.json' \
              \) 2>/dev/null | sed 's|^\./||' || true
          )

          # Allow documented example only
          filtered=()
          for f in "${hits[@]:-}"; do
            [ -z "$f" ] && continue
            if [ "$f" = ".env.example" ]; then
              continue
            fi
            filtered+=("$f")
          done

          if [ "${#filtered[@]}" -gt 0 ]; then
            for f in "${filtered[@]}"; do
              echo "::error::Forbidden credential-like file detected: $f"
            done
            exit 1
          fi
          echo "No forbidden credential-like files detected."

      - name: Scan file contents for credential patterns
        shell: bash
        run: |
          set -uo pipefail

          # The step above matches file NAMES. It catches a committed key file
          # and nothing else. An R2 access key pasted into a Dart constant
          # passes it untouched, which is the case SECURITY.md and the PR
          # template both call out by name. Filename checking and content
          # checking are two different controls; this is the second one.
          allowlist='.github/secret-scan-allowlist.txt'

          # Each entry is a label and a pattern. Labels are what reach the log,
          # never the match itself.
          patterns=(
            'AWS/R2 style access key id|AKIA[0-9A-Z]{16}'
            'Google API key|AIza[0-9A-Za-z_-]{35}'
            'PEM private key block|-----BEGIN [A-Z ]*PRIVATE KEY-----'
            'Firebase Admin service account marker|"type"[[:space:]]*:[[:space:]]*"service_account"'
            'Slack token|xox[baprs]-[0-9A-Za-z-]{10,}'
            'Generic assigned secret|(secret|password|passwd|token|apikey|api_key)[[:space:]]*[:=][[:space:]]*["'"'"'][A-Za-z0-9/+=_-]{24,}["'"'"']'
          )

          fail=0
          for entry in "${patterns[@]}"; do
            label="${entry%%|*}"
            regex="${entry#*|}"
            while IFS= read -r hit; do
              [ -z "$hit" ] && continue
              file="${hit%%:*}"
              line="$(echo "$hit" | cut -d: -f2)"
              if [ -f "$allowlist" ] && grep -qxF "${file}:${line}" "$allowlist"; then
                echo "::notice::Reviewed false positive: ${file}:${line}"
                continue
              fi
              # File and line only. The matched text is never printed, because
              # a workflow log is readable by anyone who can see the run.
              #
              # The pattern is passed with -e on purpose: the PEM one starts
              # with dashes, and without -e grep reads it as options and
              # searches for nothing at all, silently, while still exiting 0.
              echo "::error file=${file},line=${line}::Possible credential in source: ${label}. If this is a false positive, add ${file}:${line} to ${allowlist} with the reason in the pull request."
              fail=1
            done < <(
              grep -rnE --binary-files=without-match                 --exclude-dir=.git --exclude-dir=build --exclude-dir=.dart_tool                 --exclude="$(basename "$allowlist")"                 -e "$regex" . 2>/dev/null | sed 's|^\./||' | cut -d: -f1,2 || true
            )
          done

          if [ "$fail" -eq 0 ]; then
            echo "No credential patterns found in file contents."
          fi
          exit "$fail"

  flutter-format:
    name: flutter-format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: subosito/flutter-action@v2
        with:
          flutter-version-file: .fvmrc
          channel: stable
          cache: true
      - run: flutter pub get
      - name: Check formatting
        run: dart format --output=none --set-exit-if-changed .

  flutter-analyze:
    name: flutter-analyze
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: subosito/flutter-action@v2
        with:
          flutter-version-file: .fvmrc
          channel: stable
          cache: true
      - run: flutter pub get
      # The threshold is stated rather than inherited. `flutter analyze` turns
      # both of these on by default today, so they change nothing right now;
      # they are written out because a tool default is not a promise, and a
      # release that changed its mind would silently stop this job from gating.
      #
      # `dart analyze` is a different command and does not treat infos as fatal
      # at all. That is why analysis_options.yaml raises the lints that describe
      # real defects to error severity rather than relying on this threshold:
      # error is the one severity both commands stop on.
      - run: flutter analyze --fatal-infos --fatal-warnings

  flutter-test:
    name: flutter-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: subosito/flutter-action@v2
        with:
          flutter-version-file: .fvmrc
          channel: stable
          cache: true
      - run: flutter pub get
      - name: Run tests with coverage
        run: flutter test --coverage
      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-lcov
          path: coverage/lcov.info
          if-no-files-found: ignore
          retention-days: 7

  android-build:
    name: android-build
    runs-on: ubuntu-latest
    needs: [flutter-analyze, flutter-test]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - uses: subosito/flutter-action@v2
        with:
          flutter-version-file: .fvmrc
          channel: stable
          cache: true
      - run: flutter pub get
      - name: Build debug APK (unsigned verification)
        run: flutter build apk --debug
      - name: Upload debug APK
        uses: actions/upload-artifact@v7
        with:
          name: app-debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk
          if-no-files-found: ignore
          retention-days: 5

  ios-build:
    name: ios-build
    runs-on: macos-latest
    needs: [flutter-analyze, flutter-test]
    steps:
      - uses: actions/checkout@v7
      - uses: subosito/flutter-action@v2
        with:
          flutter-version-file: .fvmrc
          channel: stable
          cache: true
      - run: flutter pub get
      - name: Build iOS simulator (no signing)
        run: flutter build ios --simulator --no-codesign
